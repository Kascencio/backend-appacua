NODE_ENV=
PORT=3100
HOST=0.0.0.0
DOCKER_IMAGE=
DOCKER_NETWORK=
# Si MySQL está en el host (fuera de Docker), en Linux usa host.docker.internal.
# Requiere extra_hosts: "host.docker.internal:host-gateway" en el stack.
# Si usas docker-compose.mysql.yml, usa host mysql:
# DATABASE_URL=mysql://user:pass@mysql:3306/aqua_sonda
JWT_SECRET=
DATABASE_URL=
JWT_EXPIRES_IN=
REFRESH_TOKEN_EXPIRES_IN=
TELEGRAM_ENABLED=
TELEGRAM_BOT_TOKEN= 
TELEGRAM_WEBHOOK_SECRET=
